# # CRAN
# requiredPackages <- c("RColorBrewer", "rgeos", "jsonlite", "shiny", "Rcpp",
#                       "rmarkdown", "knitr", "base64enc", "RCurl", "yaml")
# packagesCRAN(requiredPackages, update=update_packages)
# 
# # Github
# requiredPackages <- c("basleR", "beeplot")
# packagesGithub(requiredPackages, repo_name="greenore",
#                proxy_url="webproxy.balgroupit.com", port=3128,
#                auth_token="af8bd0071e5aef71efd5aded3aee60ec68f8a41f",
#                update=update_packages)

# Clear Workspace
#----------------
rm(list = ls())
